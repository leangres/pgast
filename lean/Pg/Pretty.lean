/-
Pg.Pretty — pretty-printer from `Pg.Ast.Stmt` to PL/pgSQL source.

Receiving stub. The real `PgPretty` content arrives via Phase 1 of
the PgAst extraction (target start 2026-06-01). When Phase 1 lands,
this file's contents are overwritten with the moved
`lean/Aion/Db/PgPretty.lean` body (namespace `Aion.Db.PgPretty → Pg.Pretty`).

See the doc trail in ~/Documents/rfcs/narrative/pgast-extraction-*.md.
-/

import Pg.Ast

namespace Pg.Pretty

/-- Placeholder. Replaced in Phase 1 by the moved PgPretty content. -/
def placeholder : Unit := ()

end Pg.Pretty
